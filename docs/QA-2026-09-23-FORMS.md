# Post-release QA: name and contact forms

## Regression found and fixed
Returning from contact details to the name step remounted its control with an empty value. Initialize the name control from the previously entered lead name. Keep the input length aligned with the server's 200-character maximum.

## Checks performed
- Published quiz desktop/mobile: name Enter/send arrow, blank-name rejection, contact layout, phone/consent validation, required-email flag, personalized gift copy, no duplicated name field, no horizontal overflow.
- Browser security: fresh CSP nonces; parser-injected inline scripts and event handlers blocked; tracking sandbox cannot access parent DOM/storage, including direct navigation.
- Isolated authenticated fixture: login, editor load and actual atomic flow save; settings/leads/inbox; account/workspace/quiz removed after test.
- No open Dependabot alerts. Delivery queue inspection returned only sent jobs; last three worker HTTP responses were 200 without timeouts.
- Supabase advisor still reports the intentionally server-only RLS tables and intentionally authenticated SECURITY DEFINER functions, plus disabled leaked-password protection. These are not claimed to be zero findings.

Final regression results and full isolated submission verification are recorded in the PR.

## Remaining operational work
Backup storage, a completed backup and isolated restore drill are not established. Email alert receipt remains unverified. GitHub schedule execution is best-effort; the verified manual run does not prove timely recurring runs. Personal MFA and root credential inventory/rotation remain separate owner/platform work. The temporary previous signing key automatically stops verifying at its configured deadline and should be removed afterward. No independent penetration test or absolute-security certification has been performed.

Leaked-password protection reference: https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection
